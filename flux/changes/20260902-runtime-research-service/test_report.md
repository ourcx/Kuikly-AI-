# 研究服务运行时接入与界面收敛测试报告

## 结果

- 共享单元测试：43 条通过，0 失败。
- Kuikly 通用层与 Android 原生桥接编译成功。
- Android Debug APK 构建成功。
- `git diff --check` 通过。
- 敏感信息扫描未发现新增 Token、Cookie、密码、私钥或认证头。

## 覆盖

- 远端仓库在每次发送前动态读取服务配置。
- 保存合法 HTTPS 地址后立即切换为已配置状态。
- 非法地址保留设置面板并展示错误。
- 移除运行时配置后恢复本地模式；存在构建默认值时仍保持可用。
- 在线服务失败并降级时展示真实来源状态。

## 已知提示

构建仍会输出项目既有的 Kotlin/D8 metadata 兼容警告，但未中断 dex、打包或 APK 生成。
